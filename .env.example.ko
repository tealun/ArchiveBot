# ArchiveBot 환경 변수 설정 예제
# 이 파일을 .env로 복사하여 실제 값을 입력하세요

# ====================
# Telegram Bot 설정 (필수)
# ====================
# @BotFather에서 발급
BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# @userinfobot에서 Telegram ID 확인
OWNER_ID=0

# ====================
# Telegram 저장소 채널 설정 (선택)
# ====================
# 기본 저장소 채널 ID (필수)
CHANNEL_DEFAULT=-1001234567890

# 문서 유형별 채널 ID (선택, 설정하지 않으면 기본 채널 사용)
CHANNEL_TEXT=-1001234567891 # 텍스트 전용 채널
CHANNEL_EBOOK=-1001234567892 # 전자책 전용 채널
CHANNEL_DOCUMENT=-1001234567893 # 문서 전용 채널
CHANNEL_IMAGE=-1001234567894 # 이미지 전용 채널
CHANNEL_MEDIA=-1001234567895 # 미디어 파일 전용 채널

# NOTE 유형은 특별하며, NOTE 모드에서 생성된 개인 노트의 아카이브 채널입니다. 설정하지 않으면 TEXT 채널에 저장됩니다
CHANNEL_NOTE=-1001234567896  # 노트 전용 채널 (선택)

# 개인 직접 전송 콘텐츠 저장소 채널 (선택)
CHANNEL_DIRECT_DEFAULT=-1001234567897

# 출처-채널 매핑 설정 (유형 매핑보다 우선순위가 높음)은 config.yaml에서 설정하세요
# 태그-채널 매핑 설정 (최우선순위)은 config.yaml에서 설정하세요

# ====================
# AI API 설정 (선택)
# ====================

# API 제공자 선택 (선택, 기본값은 grok)
AI_API_PROVIDER=

# API 키 (AI 기능 사용 시)
AI_API_KEY=

# API 주소 (선택, 입력하지 않으면 기본값 사용)
AI_API_URL=

# 모델 이름 (선택)
AI_MODEL=
AI_REASONING_MODEL=

# AI 참조 콘텐츠 제외 설정 (JSON 형식)
# 제외할 채널 ID 목록, 예: [-1003497030097]
AI_EXCLUDE_CHANNELS=[]
# 제외할 태그 목록, 예: ["私密", "草稿"]
AI_EXCLUDE_TAGS=[]

# ====================
# 사용 방법
# ====================
# 1. 이 파일을 .env로 복사
# 2. 실제 BOT_TOKEN과 OWNER_ID를 입력 (필수)
# 3. 채널 ID를 입력 (Telegram 저장소를 활성화할 때)
# 4. AI_API_KEY를 입력 (AI 기능을 사용할 때)
# 5. 민감한 정보 유출을 방지하기 위해 .env를 .gitignore에 추가하세요

# ====================
# 우선순위
# ====================
# 환경 변수 > config.yaml 설정
# 환경 변수가 설정되면 config.yaml의 해당 설정을 덮어씁니다
