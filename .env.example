# ArchiveBot 环境变量配置示例
# 复制此文件为 .env 并填写真实值（如果使用环境变量配置）

# Telegram Bot 配置
# 优先级：环境变量 > config.yaml
# 如果不设置，则使用 config.yaml 中的值

# BOT_TOKEN=123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11
# OWNER_ID=123456789
# CHANNEL_ID=-1001234567890

# AI 配置（可选）
# AI_API_KEY=your_xai_api_key_here
# AI_API_URL=https://api.x.ai/v1/chat/completions
# AI_MODEL=grok-4-1-fast-non-reasoning

# 配置方式选择：
# 方式一（推荐）：使用 config.yaml 配置，不设置环境变量
# 方式二：使用环境变量覆盖敏感信息，取消上面的注释并填写真实值
